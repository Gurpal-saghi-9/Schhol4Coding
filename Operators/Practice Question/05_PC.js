// 5. Check if a number is within a range between 10 and 20 (inclusive)

let number = 15; // For testing purposes, you can replace this with a prompt in a real scenario

let res = (number >= 10 && number <= 20) ? "The number is within the range." : "The number is outside the range.";

console.log(res); // Output: The number is within the range.